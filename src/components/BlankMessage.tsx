import { Typography } from "@mui/material";

const BlankMessage = () => {
   return (
      <div>
         <Typography variant="caption">No items here so far.</Typography>
      </div>
   );
};

export default BlankMessage;
