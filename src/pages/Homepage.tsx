import React from "react";
import CustomDrawer from "../components/CustomDrawer";

const Homepage = () => {
   return (
      <div>
         <CustomDrawer />
      </div>
   );
};

export default Homepage;
