import CustomDrawer from "../components/CustomDrawer";
import { Box } from "@mui/material";

const Homepage = () => {
   return (
      <Box sx={{ height: "100%" }}>
         <CustomDrawer />
      </Box>
   );
};

export default Homepage;
